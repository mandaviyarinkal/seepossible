define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'Custom_CmsCheckout/checkout/shipping/additional-block'
        }
    });
});